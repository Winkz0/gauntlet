requests>=2.31
PyYAML>=6.0

# Google Sheet mirror (optional; sheet_sync imports lazily)
gspread>=6.0
google-auth>=2.30

# Document generation used by the per-application build_docs.py scripts.
# PDF rendering uses LibreOffice (soffice --headless --convert-to pdf).
python-docx>=1.1
